# Trimmed from the template's -Xmx8G/-XX:MaxMetaspaceSize=4G: this is a small
# app on a 16 GB machine, and the original ask caused swapping. Heap dumps
# are off so an OOM cannot fill the disk with a multi-GB .hprof.
org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
